# Scripts

This directory contains utility scripts for the AgentCohort project.

## generate_docs.py

Generates CLI documentation in Markdown format by programmatically calling all CLI commands with `--help`.

### Usage

```bash
python scripts/generate_docs.py
```

The script will generate `docs/reference.md`.
